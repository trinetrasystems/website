import { Navigate } from "react-router-dom";

// Merged into /residential-cctv-ai-surveillance — redirecting to avoid duplicate content
const SocietySecurity = () => <Navigate to="/residential-cctv-ai-surveillance" replace />;

export default SocietySecurity;
